spring:
  datasource:
    url: jdbc:h2:mem:uncomplex-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

app:
  ai:
    provider: mock
  rate-limit:
    generations-per-day: 1000
